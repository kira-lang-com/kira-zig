backends = ["hybrid", "vm", "llvm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM157"
diagnostic_title = "consuming method call on borrowed receiver"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
