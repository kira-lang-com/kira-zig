backends = ["hybrid", "vm", "llvm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM156"
diagnostic_title = "nativeStateFree requires a native state token"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
