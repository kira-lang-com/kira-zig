backends = ["vm", "llvm", "hybrid"]

[phases.check]
result = "fail"
diagnostic_code = "KMAC021"
diagnostic_title = "macro diagnostic"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
