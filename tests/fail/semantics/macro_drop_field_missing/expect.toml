backends = ["vm", "llvm", "hybrid"]

[phases.check]
result = "fail"
diagnostic_code = "KMAC025"
diagnostic_title = "dropField: no such field"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
