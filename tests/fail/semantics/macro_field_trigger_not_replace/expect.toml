backends = ["vm", "llvm", "hybrid"]

[phases.check]
result = "fail"
diagnostic_code = "KMAC029"
diagnostic_title = "field-triggered macro must replace"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
