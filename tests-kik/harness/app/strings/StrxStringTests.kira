import Foundation

// StrxStringTests.kira — deep string coverage for `kira test`.
// PREFIX: strx / Strx  (all top-level names use this prefix).
// This file imports Foundation (imports are file-scoped); harness flat namespace already has it.

// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------

function strxBorLen(s: borrow String) -> Int {
    return s.count
}

function strxMvLen(s: String) -> Int {
    return s.count
}

// Returns one of 5 strings by i%5.
// Period: "apple"(5) "go"(2) "three"(5) "by"(2) "x"(1) → sum per period = 15
function strxPk(i: Int) -> String {
    let m = i % 5
    if m == 0 { return "apple" }
    if m == 1 { return "go" }
    if m == 2 { return "three" }
    if m == 3 { return "by" }
    return "x"
}

// ---------------------------------------------------------------------------
// Structs
// ---------------------------------------------------------------------------

struct StrxBox {
    var name: String
    var code: Int

    function nameLen() -> Int {
        return self.name.count
    }

    function coded() -> Int {
        return self.code + 1
    }
}

function strxBoxBor(b: borrow StrxBox) -> Int {
    return b.name.count + b.code
}

struct StrxBoxInner {
    var word: String
}

struct StrxBoxOuter {
    var inner: StrxBoxInner
    var tag: String
    var n: Int
}

function strxOutLen(o: borrow StrxBoxOuter) -> Int {
    return o.inner.word.count + o.tag.count + o.n
}

// ---------------------------------------------------------------------------
// Enum with String payload
// ---------------------------------------------------------------------------

enum StrxLbl {
    Named(String)
    Empty
}

function strxLblLen(l: borrow StrxLbl) -> Int {
    match l {
        Named(s) -> { return s.count }
        Empty -> { return 0 }
    }
}

// Period of 3: Named("hello")(5), Empty(0), Named("ok")(2) → sum = 7
function strxLblFor(i: Int) -> StrxLbl {
    let m = i % 3
    if m == 0 { return .Named("hello") }
    if m == 1 { return .Empty }
    return .Named("ok")
}

// ---------------------------------------------------------------------------
// Literal .count tests
// ---------------------------------------------------------------------------

Test StrxLitA {
    test { return "a".count }
    expect { let e: Result<Int, TestFailure> = .Ok(1); return e }
}

Test StrxLitHello {
    test { return "hello".count }
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxLitEmpty {
    test { return "".count }
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

Test StrxLitTen {
    test { return "abcdefghij".count }
    expect { let e: Result<Int, TestFailure> = .Ok(10); return e }
}

Test StrxLitKira {
    test { return "kira".count }
    expect { let e: Result<Int, TestFailure> = .Ok(4); return e }
}

Test StrxLitSpace {
    test { return "hello world".count }
    expect { let e: Result<Int, TestFailure> = .Ok(11); return e }
}

Test StrxLitTwelve {
    test { return "123456789012".count }
    expect { let e: Result<Int, TestFailure> = .Ok(12); return e }
}

// ---------------------------------------------------------------------------
// Borrow-len helper tests
// ---------------------------------------------------------------------------

Test StrxBorLitXyz {
    test { return strxBorLen("xyz") }
    expect { let e: Result<Int, TestFailure> = .Ok(3); return e }
}

Test StrxBorLitEmpty {
    test { return strxBorLen("") }
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

Test StrxBorVar {
    test {
        let s = "reuse"
        return strxBorLen(s)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxBorVarTwice {
    test {
        let s = "ab"
        var out = strxBorLen(s)
        out = out + strxBorLen(s)
        return out
    }
    // 2 + 2 = 4
    expect { let e: Result<Int, TestFailure> = .Ok(4); return e }
}

Test StrxBorSix {
    test { return strxBorLen("abcdef") }
    expect { let e: Result<Int, TestFailure> = .Ok(6); return e }
}

Test StrxBorMixed {
    test {
        var out = strxBorLen("xy")
        out = out + strxBorLen("abc")
        return out
    }
    // 2 + 3 = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

// ---------------------------------------------------------------------------
// Move-len helper tests
// ---------------------------------------------------------------------------

Test StrxMvLit {
    test { return strxMvLen(move "consume") }
    expect { let e: Result<Int, TestFailure> = .Ok(7); return e }
}

Test StrxMvVar {
    test {
        let s = "transfer"
        return strxMvLen(move s)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(8); return e }
}

// ---------------------------------------------------------------------------
// String-returning function tests (bind result before .count)
// ---------------------------------------------------------------------------

Test StrxPkApple {
    test {
        let s = strxPk(0)
        return s.count
    }
    // "apple" = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxPkGo {
    test {
        let s = strxPk(1)
        return s.count
    }
    // "go" = 2
    expect { let e: Result<Int, TestFailure> = .Ok(2); return e }
}

Test StrxPkThreeStr {
    test {
        let s = strxPk(2)
        return s.count
    }
    // "three" = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxPkBy {
    test {
        let s = strxPk(3)
        return s.count
    }
    // "by" = 2
    expect { let e: Result<Int, TestFailure> = .Ok(2); return e }
}

Test StrxPkX {
    test {
        let s = strxPk(4)
        return s.count
    }
    // "x" = 1
    expect { let e: Result<Int, TestFailure> = .Ok(1); return e }
}

Test StrxPkWrap {
    test {
        let s = strxPk(5)
        return s.count
    }
    // 5 % 5 = 0 -> "apple" = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxPkBorBound {
    test {
        let s = strxPk(2)
        return strxBorLen(s)
    }
    // "three" = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxPkLoop20 {
    test {
        var total = 0
        var i = 0
        while i < 20 {
            let s = strxPk(i)
            total = total + s.count
            i = i + 1
        }
        return total
    }
    // 4 periods of 15 = 60
    expect { let e: Result<Int, TestFailure> = .Ok(60); return e }
}

Test StrxPkLoop40 {
    test {
        var total = 0
        var i = 0
        while i < 40 {
            let s = strxPk(i)
            total = total + s.count
            i = i + 1
        }
        return total
    }
    // 8 periods of 15 = 120
    expect { let e: Result<Int, TestFailure> = .Ok(120); return e }
}

// Return a String from the test block directly
Test StrxRetHello {
    test { return "hello" }
    expect { let e: Result<String, TestFailure> = .Ok("hello"); return e }
}

Test StrxRetPk0 {
    test {
        let s = strxPk(0)
        return s
    }
    expect { let e: Result<String, TestFailure> = .Ok("apple"); return e }
}

Test StrxRetPk3 {
    test {
        let s = strxPk(3)
        return s
    }
    expect { let e: Result<String, TestFailure> = .Ok("by"); return e }
}

// ---------------------------------------------------------------------------
// Struct with String field
// ---------------------------------------------------------------------------

Test StrxBoxNameLen {
    test {
        let b = StrxBox { name: "test", code: 0 }
        return b.name.count
    }
    // "test" = 4
    expect { let e: Result<Int, TestFailure> = .Ok(4); return e }
}

Test StrxBoxMethod {
    test {
        let b = StrxBox { name: "hello", code: 0 }
        return b.nameLen()
    }
    // "hello" = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxBoxCoded {
    test {
        let b = StrxBox { name: "", code: 99 }
        return b.coded()
    }
    // 99 + 1 = 100
    expect { let e: Result<Int, TestFailure> = .Ok(100); return e }
}

Test StrxBoxBorFn {
    test {
        let b = StrxBox { name: "abc", code: 7 }
        return strxBoxBor(b)
    }
    // 3 + 7 = 10
    expect { let e: Result<Int, TestFailure> = .Ok(10); return e }
}

Test StrxBoxMutCode {
    test {
        var b = StrxBox { name: "x", code: 0 }
        var i = 0
        while i < 10 {
            b.code = b.code + 1
            i = i + 1
        }
        return b.code
    }
    // 10
    expect { let e: Result<Int, TestFailure> = .Ok(10); return e }
}

Test StrxBoxNamePersist {
    test {
        var b = StrxBox { name: "hello", code: 0 }
        b.code = 999
        return b.name.count
    }
    // "hello" = 5 (name not changed)
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxBoxCopyIndep {
    test {
        let b = StrxBox { name: "abc", code: 5 }
        var c = b
        c.code = 99
        return b.code
    }
    // original unchanged = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxBoxLoopBuild {
    test {
        var total = 0
        var i = 0
        while i < 5 {
            let nm = strxPk(i)
            let b = StrxBox { name: move nm, code: i }
            total = total + b.nameLen()
            i = i + 1
        }
        return total
    }
    // 1 period: 5+2+5+2+1 = 15
    expect { let e: Result<Int, TestFailure> = .Ok(15); return e }
}

Test StrxBoxLoopAcc {
    test {
        var acc = 0
        var i = 0
        while i < 8 {
            let nm = strxPk(i)
            let b = StrxBox { name: move nm, code: i }
            acc = acc + b.nameLen() + b.code
            i = i + 1
        }
        return acc
    }
    // i=0..7: nameLen=5,2,5,2,1,5,2,5 sum=27; code=0+1+2+3+4+5+6+7=28; total=55
    expect { let e: Result<Int, TestFailure> = .Ok(55); return e }
}

// ---------------------------------------------------------------------------
// Array of structs with String fields
// ---------------------------------------------------------------------------

Test StrxBoxArrCount {
    test {
        var boxes: [StrxBox] = []
        var i = 0
        while i < 8 {
            let nm = strxPk(i)
            boxes.append(StrxBox { name: move nm, code: i })
            i = i + 1
        }
        return boxes.count
    }
    expect { let e: Result<Int, TestFailure> = .Ok(8); return e }
}

Test StrxBoxArrSum {
    test {
        var boxes: [StrxBox] = []
        var i = 0
        while i < 10 {
            let nm = strxPk(i)
            boxes.append(StrxBox { name: move nm, code: 0 })
            i = i + 1
        }
        var total = 0
        var j = 0
        while j < boxes.count {
            total = total + boxes[j].nameLen()
            j = j + 1
        }
        return total
    }
    // 2 periods of 15 = 30
    expect { let e: Result<Int, TestFailure> = .Ok(30); return e }
}

Test StrxBoxArrIdx3 {
    test {
        var boxes: [StrxBox] = []
        var i = 0
        while i < 10 {
            let nm = strxPk(i)
            boxes.append(StrxBox { name: move nm, code: i })
            i = i + 1
        }
        return boxes[3].name.count
    }
    // i=3 -> strxPk(3) = "by" = 2
    expect { let e: Result<Int, TestFailure> = .Ok(2); return e }
}

Test StrxBoxArrIdxCode {
    test {
        var boxes: [StrxBox] = []
        var i = 0
        while i < 6 {
            let nm = strxPk(i)
            boxes.append(StrxBox { name: move nm, code: i * 10 })
            i = i + 1
        }
        return boxes[4].code
    }
    // i=4 -> code = 40
    expect { let e: Result<Int, TestFailure> = .Ok(40); return e }
}

// ---------------------------------------------------------------------------
// Nested struct with String field
// ---------------------------------------------------------------------------

Test StrxOutLen1 {
    test {
        let o = StrxBoxOuter {
            inner: StrxBoxInner { word: "hello" },
            tag: "ab",
            n: 10
        }
        return strxOutLen(o)
    }
    // 5 + 2 + 10 = 17
    expect { let e: Result<Int, TestFailure> = .Ok(17); return e }
}

Test StrxOutLen2 {
    test {
        let o = StrxBoxOuter {
            inner: StrxBoxInner { word: "" },
            tag: "xyz",
            n: 0
        }
        return strxOutLen(o)
    }
    // 0 + 3 + 0 = 3
    expect { let e: Result<Int, TestFailure> = .Ok(3); return e }
}

Test StrxOutDirect {
    test {
        let o = StrxBoxOuter {
            inner: StrxBoxInner { word: "abcd" },
            tag: "xy",
            n: 5
        }
        return o.inner.word.count
    }
    // "abcd" = 4
    expect { let e: Result<Int, TestFailure> = .Ok(4); return e }
}

Test StrxOutTagCount {
    test {
        let o = StrxBoxOuter {
            inner: StrxBoxInner { word: "test" },
            tag: "kira",
            n: 0
        }
        return o.tag.count
    }
    // "kira" = 4
    expect { let e: Result<Int, TestFailure> = .Ok(4); return e }
}

Test StrxOutTwoFields {
    test {
        let o = StrxBoxOuter {
            inner: StrxBoxInner { word: "abcde" },
            tag: "xy",
            n: 3
        }
        return o.inner.word.count + o.tag.count
    }
    // 5 + 2 = 7
    expect { let e: Result<Int, TestFailure> = .Ok(7); return e }
}

Test StrxInnerWordLen {
    test {
        let inn = StrxBoxInner { word: "hello" }
        return inn.word.count
    }
    // 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

// ---------------------------------------------------------------------------
// String-payload enum
// ---------------------------------------------------------------------------

Test StrxLblNamed {
    test {
        let l: StrxLbl = .Named("hello")
        return strxLblLen(l)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxLblEmpty {
    test {
        let l: StrxLbl = .Empty
        return strxLblLen(l)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

Test StrxLblNamedZero {
    test {
        let l: StrxLbl = .Named("")
        return strxLblLen(l)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

Test StrxLblFor0 {
    test {
        let l = strxLblFor(0)
        return strxLblLen(l)
    }
    // Named("hello") = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxLblFor1 {
    test {
        let l = strxLblFor(1)
        return strxLblLen(l)
    }
    // Empty = 0
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

Test StrxLblFor2 {
    test {
        let l = strxLblFor(2)
        return strxLblLen(l)
    }
    // Named("ok") = 2
    expect { let e: Result<Int, TestFailure> = .Ok(2); return e }
}

Test StrxLblFor3 {
    test {
        let l = strxLblFor(3)
        return strxLblLen(l)
    }
    // 3 % 3 = 0 -> Named("hello") = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxLblLoopSum {
    test {
        var total = 0
        var i = 0
        while i < 30 {
            let l = strxLblFor(i)
            total = total + strxLblLen(l)
            i = i + 1
        }
        return total
    }
    // 10 periods of (5+0+2) = 70
    expect { let e: Result<Int, TestFailure> = .Ok(70); return e }
}

Test StrxLblArrBuild {
    test {
        var lbls: [StrxLbl] = []
        var i = 0
        while i < 15 {
            lbls.append(strxLblFor(i))
            i = i + 1
        }
        var total = 0
        var j = 0
        while j < lbls.count {
            total = total + strxLblLen(lbls[j])
            j = j + 1
        }
        return total
    }
    // 5 periods of 7 = 35
    expect { let e: Result<Int, TestFailure> = .Ok(35); return e }
}

Test StrxLblArrCount {
    test {
        var lbls: [StrxLbl] = []
        var i = 0
        while i < 9 {
            lbls.append(strxLblFor(i))
            i = i + 1
        }
        return lbls.count
    }
    expect { let e: Result<Int, TestFailure> = .Ok(9); return e }
}

// ---------------------------------------------------------------------------
// String arrays — literal and built
// ---------------------------------------------------------------------------

Test StrxArrLitIdx0 {
    test {
        let arr = ["one", "two", "three"]
        return arr[0].count
    }
    // "one" = 3
    expect { let e: Result<Int, TestFailure> = .Ok(3); return e }
}

Test StrxArrLitIdx2 {
    test {
        let arr = ["one", "two", "three"]
        return arr[2].count
    }
    // "three" = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxArrLitForIn {
    test {
        let ws = ["cat", "dog", "fish", ""]
        var total = 0
        for w in ws {
            total = total + w.count
        }
        return total
    }
    // 3+3+4+0 = 10
    expect { let e: Result<Int, TestFailure> = .Ok(10); return e }
}

Test StrxArrLitCount {
    test {
        let arr = ["a", "b", "c"]
        return arr.count
    }
    expect { let e: Result<Int, TestFailure> = .Ok(3); return e }
}

Test StrxArrBuildCount {
    test {
        var arr: [String] = []
        var i = 0
        while i < 10 {
            let s = strxPk(i)
            arr.append(move s)
            i = i + 1
        }
        return arr.count
    }
    expect { let e: Result<Int, TestFailure> = .Ok(10); return e }
}

Test StrxArrBuildSum {
    test {
        var arr: [String] = []
        var i = 0
        while i < 15 {
            let s = strxPk(i)
            arr.append(move s)
            i = i + 1
        }
        var total = 0
        var j = 0
        while j < arr.count {
            total = total + arr[j].count
            j = j + 1
        }
        return total
    }
    // 3 periods of 15 = 45
    expect { let e: Result<Int, TestFailure> = .Ok(45); return e }
}

Test StrxArrBuildIdx {
    test {
        var arr: [String] = []
        var i = 0
        while i < 5 {
            let s = strxPk(i)
            arr.append(move s)
            i = i + 1
        }
        return arr[2].count
    }
    // strxPk(2) = "three" = 5
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

Test StrxArrBuildMvLen {
    test {
        var arr: [String] = []
        var buildTotal = 0
        var i = 0
        while i < 20 {
            let s = strxPk(i)
            buildTotal = buildTotal + s.count
            arr.append(move s)
            i = i + 1
        }
        // Also sum by index access
        var indexTotal = 0
        var j = 0
        while j < arr.count {
            indexTotal = indexTotal + arr[j].count
            j = j + 1
        }
        // Both should be equal; return their sum
        return buildTotal + indexTotal
    }
    // 4 periods of 15 = 60 each -> 120
    expect { let e: Result<Int, TestFailure> = .Ok(120); return e }
}

// ---------------------------------------------------------------------------
// Per-iteration string selection by index
// ---------------------------------------------------------------------------

Test StrxSelByMod {
    test {
        var total = 0
        var i = 0
        while i < 10 {
            var cnt = 0
            if i % 2 == 0 {
                cnt = "cat".count
            } else {
                cnt = "dog".count
            }
            total = total + cnt
            i = i + 1
        }
        return total
    }
    // 5*3 + 5*3 = 30
    expect { let e: Result<Int, TestFailure> = .Ok(30); return e }
}

Test StrxCountUnderThree {
    test {
        var cnt = 0
        var i = 0
        while i < 20 {
            let s = strxPk(i)
            if s.count < 3 {
                cnt = cnt + 1
            }
            i = i + 1
        }
        return cnt
    }
    // per period of 5: "go"(2)<3 yes, "by"(2)<3 yes, "x"(1)<3 yes -> 3 per period
    // 4 periods -> 12
    expect { let e: Result<Int, TestFailure> = .Ok(12); return e }
}

// ---------------------------------------------------------------------------
// Trap tests
// ---------------------------------------------------------------------------

Test StrxTrapArrOob {
    test {
        let arr: [String] = ["a", "b"]
        let s = arr[5]
        return s.count
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}

Test StrxTrapBoxArrOob {
    test {
        var boxes: [StrxBox] = []
        boxes.append(StrxBox { name: "x", code: 1 })
        let _ = boxes[5].code
        return 0
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}

Test StrxTrapLblArrOob {
    test {
        var lbls: [StrxLbl] = []
        lbls.append(.Empty)
        let _ = strxLblLen(lbls[3])
        return 0
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}

Test StrxTrapDivZero {
    test {
        let a = 5
        let b = 0
        return a / b
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}

Test StrxTrapModZero {
    test {
        let a = 10
        let b = 0
        return a % b
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}

Test StrxTrapStrArrEmpty {
    test {
        let arr: [String] = []
        let s = arr[0]
        return s.count
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}
