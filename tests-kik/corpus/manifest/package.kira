Package KikCorpusManifest {
    let version = "0.1.0"
    let kind = .App
    let defaults = Defaults { executionMode: .Vm, buildTarget: .Host }
    let tests = Tests { backends: [.Vm, .Llvm], phase: .Both }
}
